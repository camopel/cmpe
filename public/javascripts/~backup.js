	/*
			if(ui.newPanel[0].id=="Upload") 
			{				
				$("#upload_form")[0].reset();
				var filerKit = $("#upload_file").prop("jFiler");
				filerKit.reset();
				filerKit.remove(0);
			}else */
			//$(ui.newPanel.selector).text(ui.newPanel.selector);
			//alert(JSON.stringify(ui.newPanel));